# Given length of side, calculate area of square
function divide()
    a::Float64 = pi
    b::Float64 = 4
    println("\n π/4 = $(a÷b) \n" )
end

divide() # call the function to get the action started